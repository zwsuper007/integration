eureka:
  client:
    serviceUrl:
      defaultZone: http://provider:provider-mytian0818@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: rabbitmq8145
  server:
    enable-self-preservation: false
server:
  port: 8145
spring:
  application:
    name: rabbitmq

